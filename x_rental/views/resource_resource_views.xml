<?xml version="1.0" encoding="utf-8"?>
<odoo>

	<!--
		Views
	-->
    <record id="resource_resource_view_search" model="ir.ui.view">
        <field name="name">resource.resource.search</field>
        <field name="model">resource.resource</field>
        <field name="arch" type="xml">
            <search string="Search Resource">
               <field name="name" />
               <field name="resource_type"/>
               <field name="user_id"/>
               <field name="x_product_id"/>
               <field name="company_id" groups="base.group_multi_company"/>
               <separator/>
               <filter string="Archived" name="inactive" domain="[('active','=',False)]"/>
               <group>
                    <filter string="User" name="user" context="{'group_by':'user_id'}"/>
                    <filter string="Product" name="product" context="{'group_by':'x_product_id'}"/>
                    <filter string="Type" name="type" context="{'group_by':'resource_type'}"/>
                    <filter string="Company" name="company" context="{'group_by':'company_id'}" groups="base.group_multi_company"/>
                </group>
           </search>
        </field>
    </record>

    <record id="resource_resource_view_form" model="ir.ui.view">
        <field name="name">resource.resource.form</field>
        <field name="model">resource.resource</field>
        <field name="arch" type="xml">
            <form string="Resource">
            <sheet>
                <field name="active" invisible="1"/>
                <field name="company_id" invisible="1"/>
                <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                <group>
                    <group name="user_details">
                        <field name="name"/>
                        <field name="user_id" invisible="resource_type == 'material'" required="resource_type == 'user'"/>
                        <field name="resource_type" invisible="1"/>
                        <field name="x_product_id"/>
                    </group>
                    <group name="resource_details">
                        <field name="company_id" options="{'no_create': True}" groups="base.group_multi_company"/>
                        <field name="tz"/>
                    </group>
                </group>
            </sheet>
            </form>
        </field>
    </record>

    <record id="resource_resource_view_tree" model="ir.ui.view">
        <field name="name">resource.resource.list</field>
        <field name="model">resource.resource</field>
        <field name="arch" type="xml">
            <list string="Resources" multi_edit="1" default_order="name">
                <field name="name" />
                <field name="x_product_id" optional="show"/>
                <field name="tz" optional="hide" />
                <field name="company_id" column_invisible="True"/>
                <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" optional="show"/>
            </list>
        </field>
    </record>

	<!--
		Actions
	-->
    <record id="resource_resource_action" model="ir.actions.act_window">
        <field name="name">Resources</field>
        <field name="res_model">resource.resource</field>
        <field name="search_view_id" ref="resource_resource_view_search"/>
        <field name="domain">[('resource_type', '=', 'material')]</field>
        <field name="context">{'default_resource_type': 'material'}</field>
    </record>

    <record id="resource_resource_action_view_tree" model="ir.actions.act_window.view">
        <field name="sequence" eval="10"/>
        <field name="view_mode">list</field>
        <field name="view_id" ref="resource_resource_view_tree"/>
        <field name="act_window_id" ref="resource_resource_action"/>
    </record>

    <record id="resource_resource_action_view_form" model="ir.actions.act_window.view">
        <field name="sequence" eval="15"/>
        <field name="view_mode">form</field>
        <field name="view_id" ref="resource_resource_view_form"/>
        <field name="act_window_id" ref="resource_resource_action"/>
    </record>

</odoo>
