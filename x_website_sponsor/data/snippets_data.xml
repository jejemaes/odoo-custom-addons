<?xml version="1.0" encoding="utf-8"?>
<odoo>

<data>

    <!-- Action Server for Dynamic Filter -->
    <record id="dynamic_snippet_random_sponsor_partner" model="ir.actions.server">
        <field name="name">Snippet: Random Sponsors Filter</field>
        <field name="model_id" ref="x_sponsor.model_sponsor_sponsor"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
today = datetime.date.today()
domain = [("x_partner_id.website_published", "=", True), ("x_sponsor_contract_id.x_start_date", ">=", today), ("x_sponsor_contract_id.x_stop_date", "<=", today), ("x_payment_status", "=", "paid")]

search_domain = env.context.get('search_domain', [])
if search_domain:
    domain += search_domain

sponsorship_count = model.search_count(domain)

limit = env.context.get('limit', 8)

# pseudo random as we can import random lib
offset = max(0, sponsorship_count - limit)
if offset:
    offset = datetime.datetime.now().minute % offset

sponsors = model.search(domain, offset=offset, limit=limit)
response = sponsors
]]></field>
    </record>

    <!-- Snippet for Dynamic Filter -->
    <record id="dynamic_filter_random_sponsor_partner" model="website.snippet.filter">
        <field name="name">Random Sponsors Snippet</field>
        <field name="action_server_id" ref="dynamic_snippet_random_sponsor_partner"/>
        <field name="field_names">display_name</field>
        <field name="limit" eval="16"/>
    </record>

</data>

</odoo>
